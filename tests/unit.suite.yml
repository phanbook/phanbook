# Codeception Test Suite Configuration

# suite for unit (internal) tests.
class_name: UnitTester
modules:
    enabled: [Asserts, UnitHelper, Phalcon1]
    config:
        Phalcon1:
            bootstrap: tests/bootstrap.php
            cleanup: true
            savepoints: true
